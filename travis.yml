language: node_js
node_js:
  - "9.4.0"
cache:
  directories:
  - node_modules
script:
  - gatsby build --prefix-paths
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $github_token
  local_dir: public
  on:
    branch: master
